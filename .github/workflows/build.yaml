name: Build project from sources

on:
    push:
        branches:
        - '*'
    pull_request:
        branches:
        - dev

jobs:
    test:
      uses: ./test.yaml
    build:
        needs: test
        if: success()
        runs-on: ubuntu-latest
        steps:
        - name: Run Gradle Build
          run: ./gradlew build